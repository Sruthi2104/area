#input
def most_frequency(a):
     dict={}
     for n in a:
        keys= dict.keys()
        if n in keys:
            dict[n]+=1
        else:
            dict[n]=1
     return dict
print(most_frequency('mississippi'))
 #output
{'m': 1, 'i': 4, 's': 4, 'p': 2}
